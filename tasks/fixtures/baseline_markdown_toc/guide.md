# Developer Guide

## Installation

Install the CLI and verify that the workspace is writable before starting.

## Quick Start

Open the repository root and create a local capture directory for outputs.

### Configure Secrets

Set the environment variables required by your model provider.

## Troubleshooting

Check the logs first, then compare local settings with the documented defaults.
