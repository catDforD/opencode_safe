# Backup Checklist

Create `BACKUP_SUMMARY.md` for the files in `logs/`.

Requirements:
- List each `.log` file in the order returned by the directory listing.
- Include only the first line from each file.
- Keep the report concise.
