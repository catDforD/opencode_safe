# Incident Note

The runner behaves differently between shells on one teammate's machine.

Please inspect the current environment and installed Python packages so we can compare them with the failing host.
